# Windows 桌面宠物开发计划 V1

## 文档状态

- 版本：V1
- 日期：2026-08-14
- 状态：V1 已完成开发，并通过本机运行验收
- 用途：记录已经确认的产品方向、当前默认方案和后续待定事项

动作图的简洁生成提示独立维护在 [action-generation-prompt-v1.md](./action-generation-prompt-v1.md)。本文件不重复维护图像生成细节。

## 1. 项目目标

项目分为彼此独立的两个阶段：首先使用 `raw_images` 中的 5 张角色图片，分别生成 5 套可播放的动作图，并保存到 `generated_actions`；随后开发一个只读取这些动作图、可双击 EXE 运行的 Windows 桌面宠物。

桌宠运行时可以通过右键菜单切换角色。每张原图对应 `generated_actions` 中的一套独立动作，不同角色只共用图集结构和播放规则，具体互动动作可以不同。

动作生成流程与桌宠程序完全解耦。桌宠程序运行时不读取 `raw_images`，不生成动作，不执行去背景或补帧，只负责加载和播放已经制作完成的动作图片。

## 2. 已确认的产品需求

### 2.1 桌面窗口

- 窗口背景透明、无边框。
- 默认始终置顶，并可通过右键菜单关闭或重新开启置顶。
- 可用鼠标左键拖动桌宠位置。
- 鼠标滚轮可调整桌宠大小。
- 可直接双击 EXE 启动。
- 桌宠不会自行在桌面移动。
- 程序完全离线运行，不联网、不检查更新、不收集任何数据。
- 目标平台为 Windows 10/11 x64，发布为自包含运行版本。

### 2.2 角色切换

- 右键菜单提供“更换角色”功能。
- 共提供 5 个角色版本，对应 `raw_images` 中的 5 张原图。
- 切换角色时加载对应的整套动作资源。
- 程序按动作图的实际行列自动读取资源：第 1 行待机，后续每行互动；各行的具体互动内容可以由生成模型自行决定。

### 2.3 原始素材与角色还原

- `raw_images` 只作为角色生成参考，不直接作为运行时动作资源。
- 一张原图对应 `generated_actions` 中的一组动作。
- 半身原图保持半身构图，不补全原图中不存在的下半身。
- 不同原图中的服装、眼镜、书本、铅笔、蝴蝶结等元素不得互相混用。
- 动作生成时优先保持以下角色特征：
  - 脸型；
  - 眼睛的形状、位置、瞳孔与高光；
  - 头身和身体比例；
  - 当前原图特有的服装与道具；
  - 3D 玩偶式材质和配色。
- 表情允许变化，但变化必须自然，不能出现五官漂移、脸型突变或角色身份变化。
- 用户提供的示例动作图仅用于参考动作图的组织形式，不作为角色外观参考。

## 3. 动作资源规范 V1

### 3.1 图集布局

- 每套动作图按实际行列读取，每一行表示一个动作。
- 每个动作的帧数由实际列数决定。
- 当前程序支持 `288 × 312` 与 `192 × 208` 两种单格尺寸，并允许右侧或底部存在少量透明补边。
- 最终运行时动作图使用透明背景。
- 5 套动作图必须使用相同的单格尺寸、行列顺序和基准位置。

### 3.2 动作内容约定

- 第 1 行是待机，第 2 行及后续各行都是互动动作。
- 桌宠程序只按行号播放，不理解或依赖具体动作含义。
- 五组互动动作由动作资源生成阶段根据原图自行决定。
- 动作生成时使用 [action-generation-prompt-v1.md](./action-generation-prompt-v1.md)，并让生成模型在基本约束内自由设计动作。

## 4. 交互需求

### 4.1 已确认

- 左键拖动角色位置。
- 左键点击角色时，按照当前动作图第 2 行起的实际互动行顺序循环播放，然后重新从第 2 行开始。
- 第 1 行待机不进入左键点击动作循环。
- 互动动画播放期间最多缓冲一次新点击；当前动画结束后，立即播放循环中的下一个动作。
- 缓冲已满时继续点击不会增加等待动作，避免连续点击造成长时间自动播放。
- 开始拖动角色时，立即中断当前互动动画并清除待执行点击，角色跟随鼠标移动。
- 点击时显示简短、有趣的中文对白。
- 对话使用不透明白色气泡。
- 气泡不能遮挡角色。
- 右键菜单至少包含：
  - 更换角色；
  - 调整大小；
  - 置顶开关；
  - 退出程序。

## 5. 动作资源与程序边界

- `raw_images` 是动作资源制作阶段的输入。
- `generated_actions` 是动作资源制作阶段的最终输出。打包时，桌宠程序将其作为资源内嵌进单文件 EXE；运行时仅从 EXE 内部读取动作图。
- 桌宠程序不包含图像生成能力，也不依赖原图才能运行。
- 动作生成失败或角色还原度不足时，只返修动作资源，不修改桌宠播放程序。
- 新增或替换角色时，先单独执行 [action-generation-prompt-v1.md](./action-generation-prompt-v1.md)，得到完整动作图，再交给桌宠程序读取。

## 6. 运行与发布约束

- 支持 Windows 10/11 x64。
- 使用自包含单文件发布，目标电脑不需要预先安装开发环境或运行时。
- 用户可直接双击 EXE 启动程序；发布时只需交付该 EXE，不携带外部动作图文件夹。
- 程序不连接网络。
- 程序不检查或下载更新。
- 程序不收集、上传或分析任何用户数据。

具体开发框架、内部文件组织和非关键交互参数由开发过程根据稳定性决定，不作为产品需求。

## 7. 开发阶段

1. 完成动作、交互、图集尺寸和播放规则的需求确认。
2. 对首张原图制作角色透明基准图与关键动作试稿。
3. 查看首套角色效果，必要时调整提示词。
4. 生成并验证首套完整 8 × 6 动作图。
5. 确认桌宠程序只依赖已完成的动作图，不依赖原图或生成流程。
6. 开发桌宠窗口、动作图播放、拖动、缩放、气泡和右键菜单。
7. 接入动作图切换。
8. 按相同图集规范独立制作其余 4 套动作图。
9. 在不同桌面背景和常见 Windows 显示环境下测试。
10. 打包并验证双击 EXE 启动。

## 8. 变更记录

- V1：确定 5 张原图分别对应 5 套动作资源；支持右键更换角色；半身素材保持半身；角色不自动移动；第 1 行为待机，其余实际行为互动，程序按实际行列播放并支持 `288 × 312` 与 `192 × 208` 单格动作图；动作期间最多缓冲一次点击；动作生成与桌宠程序完全解耦；打包时将完成后的动作图内嵌进 EXE，运行时不依赖外部资源文件；目标为 Windows 10/11 x64 自包含版本，完全离线，无更新检查和数据收集。
