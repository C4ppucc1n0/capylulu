# 橘子噜噜 · 完整图集

最终文件：`assets/pet-atlases/lulu-orange-v2.webp`，配套同名 `.pet.json`。

## 已验证

- 1536 × 2288、8 × 11、每格 192 × 208；v2 全部必需帧存在，未用格透明，保留第 0 行第 6 格中性姿态。
- RGBA 无损 WebP，透明像素 RGB 残留为 0；程序校验无错误、无警告。
- 按固定角色图逐帧复核：橘黄色宽吻、短肢、短裤、头顶橘子持续保留；未见裁断、额外道具或肢体脱离。
- 9 行动作及 16 方向已生成，不是用已有整张图集充当新产物。待机和招呼复用此前本轮角色的小样，其余行新生成。
- 切图按整行固定尺度；保留提起和拖动的有意位移。左拖行边缘裁切已返工；后半段注视的抬头不足已返工，并统一整行尺度。
- 项目 24 项消费端测试通过，包含新增角色配置、嵌入资源与图集读取。
- `build.ps1` 发布成功，新的角色资源已嵌入 `dist/CapyLulu/CapyLulu.exe`。该构建检查不代表交互动画已完成实机视觉验收。
- 发布 EXE 的 5 秒启动存活检查通过，仅关闭本次测试启动的进程，未操作用户已有实例。

## 效果与边界

- 待机眨眼；点击招呼；提起／反向放下；轻晃后的惊讶与恢复；合手等待；托腮专注；点头认可。左右拖动是被带动时的反应，不是步行动画；左拖幅度较轻。
- 注视四个主方向可辨，中间方向按顺时针排列；部分相邻方向差异较小。方向角是选帧编号，不是精确量测的头部转角。
- 常规动作从休息姿态展开并回收；招呼按短手势播放。任意时刻强制跨行切换是否平滑仍受播放器控制。
- 已查看原图和拼接后的顺序帧、制作每行动画 GIF；本地网页被浏览器安全策略阻止，**未完成浏览器实时播放的人工验收**。预览页供用户直接播放，不宣称实机交互效果已完整验收。
- 当前动作库语义记录对应媒体不可用；本次延续既有角色小样，采用无直接动作参考的相容设计，不声称新增动作逐帧还原源视频。

## 文件

`index.html`：动作播放与注视滑块；`contact-sheet.jpg`：整套总览；`gaze-contact.jpg`：方向对照；`*.gif`：独立动作；`validation.json`：结构校验；`generation.json`：参考、提示词及可获得的调用耗时。

使用内置图像生成工具。旧角色文件和运行时布局未修改，无提交、无 push。
